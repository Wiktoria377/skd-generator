# Klucz API Anthropic (opcjonalny — bez niego system używa ekstrakcji regexowej)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Bez tego klucza aplikacja działa w trybie demo (regex + golden rules).
# Z kluczem — dodatkowa analiza AI naruszeń art. 30 u.k.k.
